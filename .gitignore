########################
# Root / monorepo
########################

# Package managers
node_modules/
# package-lock.json ต้องอยู่ใน git — ไม่งั้นแต่ละเครื่อง npm install แล้วได้เวอร์ชันไม่ตรงกัน
# yarn/pnpm ยังกันไว้ เพราะโปรเจกต์นี้ใช้ npm ตัวเดียว ไม่ควรมี lockfile ปนกัน 2 ชนิด
yarn.lock
pnpm-lock.yaml
upload data/

########################
# Environment variables
########################

.env
.env.*
.env.local
.env.*.local

########################
# Build outputs
########################

dist/
build/
*.log

########################
# IDE / Editor
########################

.vscode/
.idea/
*.swp
*.swo
*~

########################
# OS junk
########################

.DS_Store
Thumbs.db

########################
# Databases / data
########################

*.db
*.sqlite

# Uploads
backend/uploads/
backend/storage/

# Testing
coverage/

########################
# Local tooling
########################

*.local.json
*.local.yml
*.local.js
*.claude
# Backup artifacts from agent runs
*.bak-*
*.db-shm
*.db-wal

# Database artifacts (any suffix) — เคยหลุดขึ้น public repo มาแล้ว
*.db.*
*.db-journal
*.db.gz
backend/backups/

# Backup / scratch artifacts from agent runs
*.bak
*.bak.*
*.bak[0-9]*
*.pre-*
*.CLOBBERED-*
tmp_*.js
tmp_*.ts
*_tmp.js
