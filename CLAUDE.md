# Phopy ERP — Claude Instructions

## ภาพรวม
ระบบ ERP สำหรับธุรกิจอาหาร/ร้านค้า รองรับ: Purchase Order, Purchase Request, Stock, BOM, Sales, Accounting
MCP tools เชื่อมตรงกับ DB — ทุก tool ที่ create/update มีผลทันที ระบบสร้าง Draft ให้ user ไปยืนยันใน Web UI

---

## หมวดซื้อ (Purchase) — กฎสำคัญ

### 1. บิลมีสองแบบ อ่านต่างกัน

| แบบบิล | ตัวอย่าง | วิธีอ่านราคา |
|---|---|---|
| **บิลตลาดสด** (handwritten) | "หอมป. 1.5 ขีด 10 บาท" | ตัวเลขสุดท้าย = **ราคารวมทั้งบรรทัด** → ใช้ `lineTotal` |
| **ใบกำกับภาษี/Invoice** | "หมูสับ 5 กก. 120 บาท/กก." | ราคามี "/กก." หรือ "ต่อหน่วย" = **ราคาต่อหน่วย** → ใช้ `unitPrice` |
| **ไม่แน่ใจ** | ราคาสั้นๆ ไม่มี /หน่วย | สันนิษฐานว่าเป็น `lineTotal` (ปลอดภัยกว่า) |

### 2. จำนวน — ห้ามแปลงตัวเลข เด็ดขาด

```
✗ ผิด: "1.5 ขีด" → quantity=150, unit=g   ← Claude แปลงหน่วยพร้อมตัวเลข
✓ ถูก: "1.5 ขีด" → quantity=1.5, unit=hg  ← คัดลอกตัวเลขตรงๆ แปลงแค่ชื่อหน่วย
```

**กฎ**: `quantity` = ตัวเลขที่เขียนในบิลเสมอ, `unit` = รหัสมาตรฐาน (แปลงแค่ชื่อ)

### 3. ตาราง unit → รหัสมาตรฐาน

| ไทย/ย่อ | รหัส |
|---|---|
| กิโลกรัม / กก. / กิโล / ก.ก. | `kg` |
| ขีด | `hg` |
| กรัม / ก. | `g` |
| ลิตร / ล. | `ltr` |
| มล. / ซีซี / cc | `ml` |
| ชิ้น / อัน / ตัว / ใบ | `pcs` |
| ฟอง | `egg` |
| กล่อง | `box` |
| แพ็ค / แพ๊ค / แพ๊ล | `pack` |
| ถุง | `bag` |
| ซอง | `sachet` |
| ขวด | `bottle` |
| โหล | `dozen` |
| ลัง | `case` |

### 4. ตัวอย่างการแปลงที่ถูกต้อง

```
บิลตลาด: "หอมป. 1.5 ขีด 10 บาท"
→ { description:"หอมป.", quantity:1.5, unit:"hg", lineTotal:10 }

บิลตลาด: "ไข่ไก่ 30 ฟอง 120 บาท"
→ { description:"ไข่ไก่", quantity:30, unit:"egg", lineTotal:120 }

บิลระบบ: "หมูสับ 5 กก. ราคา 120 บาท/กก."
→ { description:"หมูสับ", quantity:5, unit:"kg", unitPrice:120 }

บิลรวม: "รวม 57 บาท" → billTotal:57 (ใช้ cross-check)
```

### 5. หลังสร้าง PO/PR — แสดงสรุปเสมอ

หลังเรียก `create_draft_po` หรือ `create_purchase_request` ให้แสดง:
- รายการ + จำนวน + หน่วย + ราคาต่อบรรทัด
- ยอดรวมทั้งหมด
- บอก user ว่าสร้างเป็น DRAFT แล้ว แก้ไขเพิ่มเติมได้ที่ Web UI

ตัวอย่าง reply:
```
สร้าง Draft PO-2025-00012 แล้วครับ

• หอมป. 1.5 ขีด = ฿10
• ขนมจีน 1 กก. = ฿25
• สก๊อตเทป 2 ถุง = ฿22

รวม ฿57 — แก้ไขได้ที่ erp.phopy.net/purchase/orders
```

### 6. ถ้ายอดรวม cross-check ไม่ตรง

ถ้า user ส่ง `billTotal` (ยอดรวมจากบิล) และ sum(lineTotal) ไม่ตรงกัน:
- แจ้ง user ก่อนสร้าง: "ยอดรวมจากบิล ฿X แต่คำนวณได้ ฿Y — ยืนยันสร้างไหมครับ?"
- ถ้าต่างกันมาก (>10%) ให้ระวังเป็นพิเศษ

---

## หมวดอื่น

- **Stock**: ใช้ `record_stock_movement` สำหรับรับ/เบิก/ปรับยอด
- **BOM**: `create_bom` ใช้สำหรับสูตรการผลิต
- **Sales**: `get_sales`, `get_orders` สำหรับดูข้อมูลขาย
- **Search**: `search` ค้นหาทุกอย่างในระบบ, `get_summary` ดูภาพรวม

---

## ข้อควรระวัง
- MCP tools มีผลกับ DB ทันที — ถ้าไม่แน่ใจให้ถามก่อน
- Draft PO/PR แก้ไขได้เสมอใน Web UI ก่อน submit
- ราคาทุกอย่างเป็น **บาท (THB)**
